INCLUDE_DIRECTORIES(
  ${GLIB2_MAIN_INCLUDE_DIR}
  ${GLIB2_INTERNAL_INCLUDE_DIR}
  ${GLIBMM2_MAIN_INCLUDE_DIR}
  ${GLIBMM2_INTERNAL_INCLUDE_DIR}
  ${GTHREAD2_MAIN_INCLUDE_DIR}
  ${SIGC++_INCLUDE_DIR}
  ${MAVLINK_INCLUDE_DIR}
  ${MAVLINK_TYPES_INCLUDE_DIR}
)


PIXHAWK_EXECUTABLE(tracker tracker.cc)
PIXHAWK_LINK_LIBRARIES(tracker
  ${GLIB2_LIBRARY}
  ${GLIBMM2_LIBRARY}
  ${GTHREAD2_LIBRARY}
  ${Boost_PROGRAM_OPTIONS_LIBRARY}
  ${Boost_SYSTEM_LIBRARY}
  ${OPENCV_CORE_LIBRARY_OPTIMIZED}
  ${OPENCV_IMGPROC_LIBRARY_OPTIMIZED}
  ${OPENCV_HIGHGUI_LIBRARY_OPTIMIZED}
  ${OPENCV_VIDEO_LIBRARY_OPTIMIZED}
  ${SIGC++2_LIBRARY}
  mavconn_shm  
  mavconn_lcm
  lcm
)

PIXHAWK_EXECUTABLE(detector detector.cc)
PIXHAWK_LINK_LIBRARIES(detector
  ${GLIB2_LIBRARY}
  ${GLIBMM2_LIBRARY}
  ${GTHREAD2_LIBRARY}
  ${SIGC++2_LIBRARY}
  ${Boost_PROGRAM_OPTIONS_LIBRARY}
  ${Boost_SYSTEM_LIBRARY}
  ${OPENCV_CORE_LIBRARY_OPTIMIZED}
  ${OPENCV_IMGPROC_LIBRARY_OPTIMIZED}
  ${OPENCV_HIGHGUI_LIBRARY_OPTIMIZED}
  ${OPENCV_OBJDETECT_LIBRARY_OPTIMIZED}
  ${SIGC++2_LIBRARY}
  mavconn_lcm
  mavconn_shm
  lcm
)
